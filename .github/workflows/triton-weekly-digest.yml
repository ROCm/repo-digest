name: Weekly TRITON Digest

on:
  schedule:
    - cron: '0 20 * * 0'  # Runs every Sunday at 8:00 PM UTC
  workflow_dispatch:  # Allows manual triggering

jobs:
  digest:
    uses: ./.github/workflows/_digest.yml
    permissions:
      contents: read
    with:
      repository: triton-lang/triton
      checkout_path: triton
      config: .claude/projects/triton.md
      days: 7
      artifact_name: weekly-digest
      timeout_minutes: 60
    secrets:
      ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
      LLM_GATEWAY_KEY: ${{ secrets.LLM_GATEWAY_KEY }}
      USER_NTID: ${{ secrets.USER_NTID }}
